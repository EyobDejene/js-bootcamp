// #########   EXERCISE MINIMUM ######### //

// define function in a variable with 2 parameters for x = start and y = end
var minimun = function (x, y) {

    // call the math + the min function
    // return the result of the minimum between the two numbers
    return Math.min(x, y);
};


//display the result
console.log(minimun(0, -10));